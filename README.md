# ASES-Aspect

```bash
npm install -g sass
npm install -g uglify-js
git clone https://github.com/sistemasases/ASES-Aspect.git
cd ASES-Aspect
sh build.sh
sh deploy.sh # Compila y despliega prueba en el navegador  
```
